#pragma once

//#define WIN32_STRING(tgt, str) \
//tgt = new wchar_t[str.Length() + 1]; \
//MultiByteToWideChar(CP_UTF8, 0, str.GetRawString(), -1,  tgt, str.Length() + 1);

class ContextInfoWin32
{
	int a;
};